export enum ADDED_IN_XML {
    true = '1',
    false = '0',
}

export enum DEFAULT_ADDRESS {
    true = '1',
    false = '0',
}

export enum IS_ACTIVE {
    true = '1',
    false = '0',
}

export enum ITEM_TYPE {
    package = 'Package',
    test = 'Test',
}
